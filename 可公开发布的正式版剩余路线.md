# SkillHub 可公开发布的正式版剩余路线

> 更新日期：2026-07-30  
> 目标平台：Windows x64  
> 目标版本：SkillHub 1.0.0  
> 当前估算：约 76%  
> 目标定义：普通用户可以安全下载安装、完成首次配置、稳定使用、升级和恢复数据，维护者可以重复发布并处理失败。

## 0. 发布策略调整：先积累产品价值，再投入发行成本

由于当前暂时没有免费的 Windows Authenticode 代码签名方案，SkillHub 不以“立即完成桌面正式发行”为唯一目标，而采用以下公开发展顺序：

```text
先完善核心功能与体验
    → 发布免费 Web 版
    → 完善开源资料
    → 积累真实用户、反馈和 STAR
    → 再投入独立域名、代码签名与正式发行
```

### 0.1 核心原则

- 不为了追求进度，在没有可信签名和完整验证的情况下公开发布 unsigned Windows 安装包。
- 优先完成搜索、分类、详情、风险提示、导入导出、首次使用和错误处理等直接影响用户价值的能力。
- Web 版优先承担 Skill 的公开浏览、搜索、说明、分类、分享和文档展示；本地扫描、依赖检查和安全执行继续由桌面端或后续 Local Agent 负责。
- 免费 Web 版可以先使用 GitHub Pages、Cloudflare Pages、Vercel 等免费托管地址，不把购买独立域名作为上线前置条件。
- STAR 只是曝光信号，不是唯一成功标准；应同时关注真实使用、Issue、复用、贡献和持续反馈，禁止制造虚假互动。

### 0.2 新增公开版阶段目标

在 Phase 10–14 之外，增加以下产品推进目标：

| 阶段 | 目标 | 进入条件 | 完成标准 |
| --- | --- | --- | --- |
| Web MVP | 发布可免费访问的 SkillHub Web 版 | 核心数据模型和前端体验稳定 | 用户可以浏览、搜索、查看 Skill 详情并获得安全说明 |
| Open Source Package | 完善开源资料 | Web MVP 可公开体验 | README、截图、Demo、安装说明、贡献指南、隐私和安全说明齐全 |
| Adoption Validation | 积累真实用户和反馈 | 公开仓库与 Web 版可访问 | 有真实使用记录、Issue/讨论或贡献，不以虚假 STAR 作为验收依据 |
| Formal Release Investment | 评估域名、签名和正式发行 | 产品价值和用户反馈已得到验证 | 再决定购买独立域名、申请代码签名并开放稳定版下载 |

### 0.3 当前产品路线上的优先级

1. 先完成桌面端核心功能、首次使用体验、安全执行边界和数据可靠性。
2. 抽离适合公开展示的前端能力，构建不依赖本地文件权限的 Web MVP。
3. 使用免费托管地址发布 Web MVP，并同步完善 GitHub 仓库资料。
4. 通过真实用户反馈修复问题和收敛范围，暂不盲目扩展市场、账号、云同步等复杂能力。
5. 当项目形成稳定的使用和社区信号后，再恢复代码签名与 Windows 正式发行投入。

## 1. 当前基线

已经完成：

- Skill 扫描、导入、查询、编辑和批量操作。
- FTS5 搜索、搜索建议、搜索历史和最近查看。
- 分类、标签、收藏和 AI 分类冲突处理。
- 深色、浅色、自定义主题和中英文界面。
- 安全 Skill 执行闭环：显式声明、确认、allowlist、取消、timeout、输出截断、进程树清理和 audit。
- Tauri Updater 状态管理、下载、取消、安装、重启和失败提示。
- Phase 9 Windows x64 运行时及 Desktop Visual QA。
- 生产签名 workflow 的代码基础。

尚未达到正式发布的根本原因：

- 生产签名依赖的 updater 私钥、Azure signing 资源和 GitHub Actions 权限尚未完成真实验证。
- 首次启动、目录配置和运行依赖检查还不够适合普通用户。
- 数据备份、恢复、损坏处理和迁移验证还没有形成完整闭环。
- 缺少覆盖真实安装、升级、卸载、回滚和长时间使用的 1.0.0 Release Candidate 验收。
- 用户文档、隐私说明、发布说明和问题反馈入口尚未形成正式交付物。

## 2. 1.0.0 完成标准

只有同时满足以下条件，才能称为“可公开发布的正式版”：

### 安装与身份

- Windows 安装包具有有效 Authenticode 签名和可信时间戳。
- 用户下载后可以验证发布者身份和 SHA-256。
- 安装、覆盖安装和卸载行为明确，不误删用户数据。

### 更新与供应链

- updater 私钥与已经发布到客户端的公钥保持连续。
- GitHub Actions 能生成安装包、updater artifact、`.sig`、`latest.json` 和 SHA-256。
- 签名、构建、校验或上传任一步失败时，不产生半成品正式 Release。
- 合法更新可安装，签名错误的更新必须被拒绝。

### 首次使用

- 用户无需理解项目目录结构即可添加或选择 Skill 目录。
- 空目录、无 Skill、权限不足和路径失效都有明确说明。
- Git 等执行依赖缺失时，在执行前给出可操作提示。
- 应用不会要求用户手工修改数据库或 JSON 才能完成基础使用。

### 数据安全

- 用户可以导出备份并在新安装中恢复。
- 数据库损坏、迁移失败和导入失败不会静默丢失数据。
- 升级、回滚和覆盖安装后，Skills、分类、标签、收藏、设置、历史和 audit 保持一致。
- 不在日志、错误、备份或文档中保存 secret。

### 稳定性

- 关键流程具有自动化测试或可复现的运行时验收记录。
- 并发扫描、分类、执行、取消和应用退出不会死锁或残留子进程。
- 5,000 个 Skill 的代表性数据集仍可正常搜索、筛选和打开页面。
- 深色、浅色、中文、英文、窄窗口和长文本状态可用。

### 发布交付

- README、安装说明、首次使用说明、备份恢复说明和安全说明完整。
- Release notes 列出功能、修复、已知限制和升级要求。
- GitHub Issues 或等价渠道可以接收问题反馈。
- 正式版本和测试版本不会混淆。

## 3. 剩余阶段总览

| 阶段 | 目标 | 完成后预计进度 | 预计投入 |
| --- | --- | ---: | ---: |
| Phase 10 | 生产签名与 Release 闭环 | 84% | 3–7 天，取决于证书和云资源 |
| Phase 11 | 首次使用与环境诊断 | 89% | 4–7 天 |
| Phase 12 | 数据备份、恢复与迁移安全 | 94% | 5–10 天 |
| Phase 13 | 稳定性、安全和真实数据 QA | 98% | 5–10 天 |
| Phase 14 | 1.0.0 Release Candidate 与公开发布 | 100% | 3–7 天 |

以上时间按单人开发估算，不包含代码签名证书审核、Azure 资源审批和外部服务等待时间。

## 4. Phase 10：生产签名与 Release 闭环

### 目标

让正式安装包具备可信发布者身份，并让 Tauri Updater 使用连续、可恢复的密钥完成自动发布。

### 当前状态

- workflow 代码基础已存在。
- 当前 updater 公钥和 endpoint 必须保持稳定。
- 本地 unsigned QA 与生产 Release 已明确区分。
- 外部 Secret、Azure signing 资源和真实 GitHub Release 尚待验证。

### 必须完成

- 确认现有 updater 私钥与当前公钥匹配。
- 将 updater 私钥、密码和 Azure signing 配置放入受保护的 GitHub Secrets。
- 为 updater 私钥建立离线加密备份和恢复说明。
- 完成 Windows Authenticode SHA-256 签名和 RFC3161 时间戳。
- 固定 GitHub Actions 依赖版本或审核后的 commit SHA。
- 生成安装包、updater artifact、`.sig`、`latest.json` 和 SHA-256。
- 在干净 VM 验证旧版本到新版本的成功升级。
- 验证错误 signature 被拒绝，旧版本仍可启动。

### Release Gate

- `Get-AuthenticodeSignature` 返回 `Valid`。
- updater 签名验证通过。
- GitHub Release 资产完整。
- 升级、失败拒绝、回滚和数据保留全部通过。

### 禁止提前做

- 原 updater 私钥未确认前不得替换公钥。
- 不得把本地 unsigned 安装包公开发布。
- 不得把证书、私钥或密码写入仓库。

## 5. Phase 11：首次使用与环境诊断

### 目标

让第一次使用 SkillHub 的普通用户不需要阅读开发文档，也能完成目录配置、扫描和安全执行。

### 必须完成

- 首次启动引导：
  - 选择 Skill 目录。
  - 解释扫描范围。
  - 显示扫描结果。
- 完善空状态：
  - 没有配置目录。
  - 目录为空。
  - 目录不存在。
  - 没有合法 `SKILL.md`。
- 增加环境诊断：
  - allowlist executable 是否可找到。
  - 文件和目录权限。
  - 数据库是否可写。
  - updater 是否可访问。
- 执行前发现依赖缺失，而不是在 spawn 后才显示 `program not found`。
- 提供一个安全、不可写入用户数据的示例 Skill。
- 所有错误使用中英文可操作文案，不暴露内部敏感路径。

### 验收标准

- 干净 VM 首次启动可以在 5 分钟内完成配置并看到第一个 Skill。
- 缺少 Git 时能在执行前明确提示安装或配置要求。
- 用户不需要手动编辑数据库或导入测试 JSON。
- 空状态、失败状态、窄窗口和主题切换通过 Desktop Visual QA。

## 6. Phase 12：数据备份、恢复与迁移安全

### 目标

确保用户即使遇到升级失败、数据库损坏或更换电脑，也能恢复长期积累的数据。

### 必须完成

- 定义正式备份格式和版本。
- 备份以下数据：
  - Skills 元数据。
  - 分类。
  - 标签及关联。
  - 收藏。
  - 设置。
  - 搜索历史和最近查看。
  - categorization history。
  - execution audit。
- 恢复前验证格式、版本和数据边界。
- 恢复操作提供预览和明确确认。
- 跨表写入使用事务，失败时完整回滚。
- 启动时检测数据库损坏和 migration 失败。
- 在修复或迁移前生成可恢复备份。
- 定义覆盖安装、卸载、回滚和换机迁移时的数据目录行为。
- 不备份 API key、token、私钥或其他 secret。

### 验收标准

- 旧版本数据可以升级到当前版本。
- 代表性备份可以恢复到干净安装。
- 损坏备份、版本不兼容和中途失败不会污染现有数据。
- 重复恢复不会产生重复关联或错误 audit。
- 恢复前后数据数量和关键字段一致。

## 7. Phase 13：稳定性、安全和真实数据 QA

### 目标

在发布前发现会影响真实用户的崩溃、性能、安全和可用性问题。

### 必须完成

- 使用 5,000 个 Skill 的代表性数据集验证：
  - 启动。
  - 扫描。
  - 搜索。
  - 筛选。
  - 标签。
  - 分类冲突。
- 做连续运行和重复操作测试：
  - 多次扫描。
  - 多次导入和导出。
  - 多次执行、取消和 Retry。
  - 反复启动和退出。
- 检查：
  - 主线程阻塞。
  - async lock contention。
  - 数据库锁。
  - 内存和句柄增长。
  - 子进程残留。
- 完成依赖和供应链审计。
- 复查 Tauri capabilities、CSP、文件访问范围和 updater 权限。
- 统一错误脱敏和用户可见错误分类。
- 覆盖中英文、长文本、深浅主题、最小窗口和键盘操作。

### 验收标准

- 所有 Required Checks 通过。
- 无 P0/P1 缺陷。
- P2 缺陷必须有明确处置结论。
- 关键流程无崩溃、死锁、数据库污染和子进程残留。
- 已知限制不会造成数据丢失或安全绕过。

## 8. Phase 14：1.0.0 Release Candidate 与公开发布

### 目标

冻结功能，完成候选版本验收，并发布可以长期维护的 SkillHub 1.0.0。

### 必须完成

- 冻结新功能，只处理发布阻塞缺陷。
- 统一版本号、应用名称、发布者和安装包元数据。
- 编写：
  - README。
  - 安装说明。
  - 首次使用说明。
  - 安全执行说明。
  - 备份恢复说明。
  - 隐私说明。
  - 已知限制。
  - Release notes。
- 从正式 workflow 生成 Release Candidate。
- 在干净 Windows x64 VM 完整执行：
  - 安装。
  - 首次启动。
  - 扫描。
  - 搜索和管理。
  - 安全执行。
  - 备份和恢复。
  - 自动更新。
  - 卸载和重新安装。
- 保存版本号、SHA-256、签名状态和验收记录。
- 建立 GitHub Issues 模板和问题反馈流程。

### 1.0.0 Release Gate

- Phase 10–13 全部 `COMPLETE`。
- 所有签名和哈希验证通过。
- 干净环境端到端验收通过。
- 数据保留和恢复通过。
- 无公开发布阻塞缺陷。
- 文档和 Release notes 完整。

## 9. 跨阶段规则

- 每个 Phase 开始前更新 `Tech-Spec.md`。
- 每个 Phase 完成后更新 `MEMORY.md`。
- 不把“代码已实现”描述为“外部环境已验证”。
- 不为了赶进度降低签名、数据恢复和安全执行标准。
- 一次只推进一个主要用户问题。
- 发现 P0/P1 问题时暂停后续 Phase，先修根因。
- 未通过 Release Gate 时统一使用：

```text
Implementation Complete
Verification Pending
```

- 只有全部完成时使用：

```text
COMPLETE
```

## 10. 每个阶段的固定验证命令

```powershell
pnpm exec tsc --noEmit
pnpm run lint
pnpm run build
cargo build --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml
```

涉及生产发布时还必须验证：

```powershell
Get-AuthenticodeSignature "<installer-path>"
Get-FileHash "<installer-path>" -Algorithm SHA256
```

## 11. 需要用户提供的外部条件

- GitHub 仓库 Release 和 Actions 管理权限。
- 与现有 updater 公钥匹配的私钥，或确认其安全存储位置。
- Windows 可信代码签名证书或 Azure Trusted Signing 资源。
- 用于 Release Candidate 的干净 Windows x64 VM。
- 正式发布者名称、支持渠道和隐私说明所需信息。
- DeepSeek 真实端到端验证所需的运行时配置；Secret 值不得进入项目文件。

## 12. 推荐执行顺序

严格按以下顺序推进：

```text
Phase 10 生产签名与 Release
    ↓
Phase 11 首次使用与环境诊断
    ↓
Phase 12 备份、恢复与迁移
    ↓
Phase 13 稳定性、安全与真实数据 QA
    ↓
Phase 14 Release Candidate
    ↓
SkillHub 1.0.0 正式发布
```

当前下一步是完成 Phase 10 的外部签名资源配置和真实 GitHub Release 验收。在 Phase 10 通过前，不进入公开发布。
