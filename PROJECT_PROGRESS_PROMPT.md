# SkillHub 项目阶段总结与后续开发提示词

> 更新时间：2026-07-18
>
> 用途：作为后续 AI 开发任务的项目上下文、阶段复盘和执行约束。阅读本文件后，先分析现状，再开始修改；不要重复已经完成的工作。

## 1. 项目目标

SkillHub 是一个基于 Tauri 2 的桌面 Skill 管理工具，目标是让用户可靠地导入、搜索、浏览、分类、标记和执行 Skill，并保留可追溯的本地数据与审计记录。

核心价值不是堆积功能，而是降低 Skill 的发现成本、理解成本和维护成本，同时保证本地数据安全、分类结果可纠正、操作过程可追溯。

## 2. 当前技术状态

- 前端：React 19、TypeScript、Vite、Tailwind CSS、Zustand。
- 桌面与后端：Tauri 2、Rust、rusqlite、bundled SQLite。
- 包管理：pnpm，`pnpm-lock.yaml` 是依赖事实来源。
- 主要目录：`src/` 为前端；`src-tauri/src/` 为 Tauri 命令、数据库、模型和分类逻辑。
- 重要约束：保持 `skills` 表的 10 列契约、保持 `Layout` 和 `navItems` 结构稳定、不要擅自改变分类并发和重试行为。

## 3. 已解决的问题

### 3.1 搜索能力不足

问题：原有 `LIKE` 查询难以支持中英文全文检索，搜索结果与规模增长后的性能不稳定。

解决：使用外部内容 FTS5 虚拟表 `skills_fts`，以 `skills.rowid` 关联主表，并使用 `unicode61` 分词器；批量导入、批量替换和单条更新后统一重建索引。

用户影响：搜索更适合中文和英文混合内容，查询逻辑更集中，也避免了对主表结构的破坏性改动。

### 3.2 用户重复搜索和重复打开 Skill

问题：用户无法快速回到刚才搜索过的内容或最近查看的 Skill。

解决：新增 `search_history` 和 `recent_views` 两类数据；搜索历史最多保留 50 条，最近查看最多保留 30 条；搜索框下拉层合并搜索建议与历史记录，Dashboard 展示最近查看区域。

用户影响：减少重复输入和页面跳转，形成更短的回访路径。

### 3.3 分类结果缺少人工组织能力

问题：单一分类不足以表达用户自己的知识体系，用户需要按主题、用途或项目自定义组织 Skill。

解决：新增 `tags` 与 `skill_tags` 关联表，支持创建、分配、移除、筛选和批量添加标签；标签不改动原有 `skills` 表。

用户影响：用户可以在保留 AI 分类的同时建立自己的多维组织方式。

### 3.4 AI 分类结果可能与人工判断冲突

问题：AI 重新分类后，用户不知道哪些结果需要复核；列表和冲突总数也可能出现口径不一致。

解决：

1. 使用分类历史表记录 AI 和人工操作。
2. 用同一个 `ROW_NUMBER()` 查询规则生成冲突列表和冲突数量。
3. 使用事务同时写入分类结果和审计历史，失败时自动回滚。
4. 通过一次 `resolve_conflicts(ResolveConflictsRequest)` 批量提交用户选择。
5. Dashboard 在加载和 AI 分类完成后刷新冲突数量。
6. 只要最新历史记录晚于最新 AI 结果，即视为已人工处理，即使用户最终保留 AI 给出的分类也算已确认。

用户影响：冲突状态可解释、可批量处理、可审计，不会因为“最终分类值相同”而误判为未处理。

### 3.5 Skill 执行存在安全风险

问题：Markdown 中的代码块不能被默认为可执行命令，否则容易把展示内容误当成操作指令。

解决：只接受显式 `execution` YAML front matter；验证命令、字符串参数、相对且不可遍历的工作目录、1–3600 秒超时和强制确认；当前只生成不可执行预览，真实进程执行仍被关闭。

用户影响：先建立安全边界和用户确认机制，避免在执行能力尚未定义清楚前引入高风险行为。

### 3.6 主题和更新器缺少可靠基础

问题：自定义主题需要实时预览但不能污染持久化设置；更新器若缺少签名、端点和发布权限，不能伪装成已可用。

解决：

- 自定义主题复用已有颜色字段，校验六位十六进制颜色，预览即时生效，只有保存设置时持久化。
- 更新器配置了 Windows x64 stable 发布目标、GitHub Releases `latest.json` 端点、公钥和签名工作流；私钥只通过 GitHub Actions secrets 提供，不写入仓库。
- 设置页显示当前版本和真实可用状态，不对未验证的更新能力做虚假承诺。

## 4. 解决这些问题的共同思路

### 4.1 先保护既有契约，再扩展能力

新功能优先通过新增表、关联表、索引、命令和局部 UI 实现，避免修改核心 `skills` 表、全局布局和既有导航。这降低迁移风险，也让功能可以独立验证和回滚。

### 4.2 用单一事实来源消除状态分叉

冲突列表和数量共用查询规则；前后端使用显式请求结构；搜索使用 FTS5 作为检索事实来源；历史记录作为审计事实来源。任何面向用户的状态都应尽量由同一套规则派生，避免“卡片显示一个数字、列表显示另一批数据”。

### 4.3 把不可逆操作放在边界上

数据库写入使用事务；执行命令必须显式声明和确认；发布更新使用签名；密钥使用环境配置。原则是：越接近外部副作用，验证和授权越严格。

### 4.4 先修根因，再补 UI

例如，冲突问题不是简单增加一个红点，而是先定义“什么叫已处理”、统一历史排序和事务边界，再让 UI 展示冲突数量和批量操作。这样 UI 不会掩盖数据规则问题。

### 4.5 每个阶段都要留下可验证证据

功能完成不能只看代码存在，还要通过类型检查、构建、Lint、Rust 构建和测试；数据库功能还要覆盖空数据、冲突、回滚、排序和性能边界；外部服务验证受凭据限制时，要明确标注为未完成，而不是模拟成功。

## 5. 仍需解决的问题

### 5.1 安全执行能力仍未开放

当前只有执行预览，没有真正执行。后续需要先定义：命令 allowlist、参数策略、权限模型、工作目录沙箱、取消机制、输出截断、超时处理、错误展示和审计日志。

建议思路：先实现“只允许明确配置的安全命令 + 每次用户确认 + 非管理员权限运行”的最小闭环，再逐步增加能力；不要直接开放任意 shell。

### 5.2 更新器缺少真实发布验证

配置已完成，但仍需要推送工作流、发布带签名的版本，并在干净 Windows 环境验证检查、下载、签名校验、安装和失败回滚。

建议思路：用测试版本完成一次真实发布闭环，记录版本号、安装包哈希和更新日志；验证失败时保留旧版本可启动。

### 5.3 DeepSeek 等 AI 服务仍依赖运行时配置

分类端到端验证需要用户提供运行时 API 配置。项目文件、日志和 `MEMORY.md` 中不得写入任何 secret。

建议思路：先用 mock 或固定代表性响应覆盖协议、错误、超时和重试；获得配置后再做一次真实服务验证，并单独记录外部依赖结果。

### 5.4 桌面端视觉验证仍受运行环境影响

静态代码和构建检查不能替代 Tauri 运行时视觉检查。需要验证桌面窗口、中文/英文切换、长文本、搜索下拉、标签弹窗、冲突处理和更新状态。

建议思路：优先覆盖主流程和高风险状态，使用固定数据做截图对照，再接入真实数据；发现文字截断、遮挡或操作不可见时，先修布局再继续功能集成。

### 5.5 项目文档需要继续保持同步

每完成一个长期有效的架构决策、外部依赖结论或用户纠正，都应更新 `MEMORY.md`；阶段目标、验收标准和风险应记录在对应 `Tech-Spec.md` 或阶段文档中。

## 6. 后续 AI 开发提示词

将以下内容作为下一阶段任务输入：

```text
# Phase X 开发任务

## 1. 目标

在不破坏 SkillHub 既有数据契约和用户流程的前提下，完成：<填写一个明确目标>。

## 2. 当前状态

已有：
- FTS5 全文搜索、搜索历史、最近查看和搜索建议已完成。
- 标签系统已完成，标签使用独立关联表，不改变 skills 表。
- AI 分类冲突具备统一查询、事务写入、批量解决和刷新机制。
- Skill 执行目前只支持安全预览，不允许真实进程执行。
- 自定义主题基础和 GitHub Releases 更新器配置已完成，但真实发布/端到端验证仍待执行。

不要修改：
- `skills` 表的 10 列契约。
- `Layout` 结构和 `navItems`，除非本阶段明确要求。
- 分类器并发、重试行为和无关用户改动。
- 任何 secret、token、私钥或 `.env` 内容。

## 3. 用户体验目标

用户应该感觉：功能状态可信、操作结果可解释、失败可以恢复、AI 结果可以被人工纠正，且不会因为新增能力而增加不必要的复杂度。

## 4. 技术要求

必须：
- 先阅读 `AGENTS.md`、`MEMORY.md`、相关 `Tech-Spec.md` 和目标代码。
- 先定义完成标准、风险和验证方式，再修改代码。
- 复用现有模式和依赖，保持 TypeScript strict，禁止无理由引入 `any`。
- 外部输入在信任边界校验；数据库跨表写入使用事务；危险操作默认关闭或需要明确确认。
- 只修改与目标直接相关的文件，并同步长期有效的项目记忆。

## 5. 执行流程

Step0 分析现状：确认已有实现、数据契约、调用链和未覆盖边界。
Step1 后端：先定义数据库/命令/错误处理，确保失败不会留下半成品状态。
Step2 API：明确请求和响应结构，保持前后端命名和类型一致。
Step3 前端：先实现固定数据下的主要状态，再接入真实数据和错误状态。
Step4 测试：运行类型检查、Lint、构建、Rust 构建和测试；必要时做运行时或截图验证。

## 6. 验收标准

功能：目标流程成功、空状态和失败状态可用，重复操作不会产生错误数据。
代码：无 secret、无无理由的依赖和抽象；数据库写入可回滚；类型和错误边界明确。
UI：无文字截断、重叠、遮挡；主要操作可发现；中英文和长文本状态可用。
验证：报告每个检查的通过/失败结果；无法验证的外部条件必须明确列出。

## 7. 输出格式

每一步报告：
修改文件：
原因：
测试：
风险：
未更新 `MEMORY.md` 前，不要宣称项目上下文已同步。
```

## 7. 下一阶段优先级

1. 先完成安全执行的最小权限模型和测试，不直接开放任意命令。
2. 完成一次真实的 Windows 更新发布闭环。
3. 补齐 AI 服务 mock/真实端到端验证和失败场景。
4. 在 Tauri 运行环境中做主流程视觉验收。
5. 持续维护 `MEMORY.md`、阶段规格和验收记录。

## 8. 文档维护规则

- 新增阶段开始前，更新目标、范围、验收标准和风险。
- 发现根因或重要取舍时，写入 `MEMORY.md`，不要只写在聊天记录中。
- 完成后记录实际修改文件和验证结果。
- 不把“配置已写入”描述为“真实服务已验证”。
- 不记录任何 secret 值，只记录 secret 的配置位置和使用方式。

## 9. 当前状态与验证边界

以下状态用于区分“代码已配置”“本地已验证”和“依赖外部环境验证”。后续任务不得把前两者混写成第三者。

| 能力 | 当前状态 | 已确认内容 | 尚未确认 |
| --- | --- | --- | --- |
| FTS5 搜索、建议、历史记录、最近查看 | 已实现 | 数据表、查询、索引重建和前端入口已接入 | 大规模真实数据下的桌面端体验 |
| 标签系统 | 已实现 | 独立关联表、筛选、批量添加和 i18n 已接入 | 长时间使用后的数据迁移兼容性 |
| 分类冲突处理 | 已实现 | 统一查询规则、事务、回滚、批量解决和性能覆盖已完成 | 真实 AI 服务端到端调用 |
| Skill 执行 | 安全预览阶段 | 显式声明、参数校验、工作目录校验和确认要求已完成 | allowlist、能力模型和真实进程执行 |
| 自定义主题 | 基础能力已实现 | 六位十六进制校验、即时预览和保存持久化已完成 | 更多主题语义和跨平台视觉验证 |
| Tauri 更新器 | 已配置待发布验证 | Windows x64 stable、签名配置和 GitHub Actions 工作流已存在 | 推送 tag、生成 release、安装更新和失败回滚 |

### 9.1 每次任务的最低验证清单

根据改动范围选择并报告以下检查，不要只报告“代码已修改”：

- 前端：`pnpm exec tsc --noEmit`、`pnpm run lint`、`pnpm run build`。
- 后端：`cargo build --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`。
- 数据库：覆盖空数据、重复操作、冲突、回滚、排序和边界规模；涉及跨表写入时确认事务范围。
- UI：在可用的 Tauri 运行环境中检查窗口尺寸、中英文、长文本、空态、加载态、失败态和主要操作可见性。
- 外部依赖：明确写出“未验证”的原因（缺少 API 配置、未推送 release、运行环境不可用等），不得用 mock 结果代替真实验证结论。

### 9.2 任务范围控制

- 一次任务只解决一个主要用户问题；若需要跨领域变更，先拆成可独立验收的子任务。
- 不因顺手而重构无关模块、升级依赖或改变既有 UI 结构。
- 新增数据库字段或表前，先说明迁移策略、旧数据行为和回滚方式。
- 新增 Tauri command 前，先写清 request、response、错误边界和前端调用方。
- 涉及安全执行、更新发布或外部 AI 服务时，必须先补规格和风险清单，再实现。

## 10. 推荐后续路线

按风险而不是按“看起来容易”排序：

1. 完成 Skill 安全执行的最小闭环：allowlist、能力声明、确认、取消、超时、输出截断、审计记录和失败回滚；默认仍关闭任意 shell。
2. 完成一次隔离的 Windows 更新发布演练，记录版本号、安装包哈希、签名校验、更新日志和失败回滚结果。
3. 为 AI 分类补齐 mock 协议测试，再在具备运行时配置后进行一次真实端到端验证；单独记录外部服务结果。
4. 在 Tauri 运行环境中完成主流程视觉验收，重点检查中英文、长文本、标签弹窗、冲突处理和更新状态。
5. 将本轮架构决策、验证结果和遗留风险同步到 `MEMORY.md` 与对应阶段规格文档。

## 11. 后续任务启动前检查

开始任何新阶段前，AI 必须按以下顺序执行：

1. 阅读项目根目录的 `AGENTS.md`、`MEMORY.md`、`Tech-Spec.md` 和本文件。
2. 检查工作区是否存在用户未提交改动，避免覆盖无关修改。
3. 定位目标功能的前端入口、Tauri command、数据库函数、模型和测试。
4. 写出完成标准、影响范围、风险和验证命令；复杂任务先更新规格文档。
5. 实现最小改动并复用现有模式。
6. 完成验证后，更新本文件的状态表和 `MEMORY.md`；若验证受外部条件限制，明确列出限制。
